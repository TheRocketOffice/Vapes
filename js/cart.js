// cart.js - Manages cart interactions
document.addEventListener('DOMContentLoaded', () => {
    const cartContent = document.getElementById('cart-content');
    cartContent.innerHTML = '<p>Sample item in your cart.</p>';
});
